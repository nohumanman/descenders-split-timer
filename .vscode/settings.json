{
    "python.analysis.extraPaths": [
        "${workspaceFolder}/src"
    ], // we're using 'common' for global stuff
    "python.autoComplete.extraPaths": [
        "${workspaceFolder}/../common"
    ],
}